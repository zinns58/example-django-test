# homecareProjectServer

# deploy mode
> docker-compose up --build

---

# debug mode
> cd project

# pip module 설치
> pip install -r config/requirements/debug/txt

# 서버 실행
> python manage.py runserver